This program validates user input if it is a 10 digit phone number.If user gives anyonther input apart from the 10 digit integer, 
it validates and throw validation message as  "Incorrect Number format!".

Assumptions:
User provides only 10 digit numbers.
User should get exception when the input is anything other than integers are given.(strings,Special characters).